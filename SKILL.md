---
name: especialista-asistencia-viajero
description: ESPECIALISTA APP ASISTENCIA VIAJERO INS — App de asistencia post-venta para clientes del Seguro INS Viajero con Asistencia Autoexpedible (registro SUGESE P19-57-A01-972 V5), marca Seguros Digitales SDI. Stack HTML + Tailwind CDN + Lucide, single-file vanilla (NO React, sin build, sin npm). Dos caras en el MISMO repo/sitio: (1) `index.html` pública = portal del cliente (contactos de emergencia, paso a paso de reclamos, Wi-Fi Call, coberturas, vigencia/prórroga, cross-sell Comprá/Recomendá); (2) `/agente/` privada = consola del agente (login Google + whitelist) que arrastra o carga el PDF de la póliza, extrae datos con pdf.js, arma y envía el correo al cliente por Gmail API (multi-viajero, encabezado navy solo-texto, monto de gastos médicos por viajero, CTA "Comprar de nuevo" al form INS) y genera 2 plantillas de WhatsApp editables vía web.whatsapp.com/send. EN PROD appasistenciainsviajero.netlify.app (auto-deploy desde main). Repo jhernandez-vibecode/APP-ASISTENCIA-INS-VIAJERO. Usar este skill cuando JC pida cualquier cambio, bug o mejora a la app de asistencia viajero, ya sea la página del cliente o la consola del agente.
---

# Especialista App Asistencia Viajero INS

Contexto completo del proyecto para retomar trabajo sin perder contexto. Leer COMPLETO antes de tocar código.

## Qué es

App de **asistencia post-venta** para clientes que YA compraron el Seguro INS Viajero con Asistencia. Hermana "viajero" de [especialista-asistencia-autos], pero **vanilla JS** (no React). Tiene **dos caras en el mismo repo/sitio/dominio**:

1. **`/` (index.html) — PÚBLICA, del cliente.** El agente la comparte por correo/WhatsApp. El cliente ve contactos de emergencia, paso a paso de reclamos, guía Wi-Fi Call, coberturas/exclusiones, vigencia/prórroga y cross-sell. No configura nada.
2. **`/agente/` — PRIVADA, del agente (JC).** Consola con login Google + whitelist para preparar y enviar la documentación de pólizas ya emitidas. El cliente NUNCA la ve.

La consola enlaza a la página pública para reclamos/contactos → "economía de código" (no se duplica esa info). Fue pedido expreso de JC: reusar la sección de reclamos de la pública en vez de duplicarla en la consola.

## Fuente oficial de los datos (REGLA 🔴)

Todo dato del producto que aparezca en la app (coberturas, exclusiones, montos, contactos, plazos) debe poder rastrearse a **`C:/Users/segur/Viajero Asistencia/_INFO-OFICIAL-VERIFICADA.md`**, junto con los 6 PDF oficiales del INS en esa misma carpeta (Condiciones Generales · Kit Cliente · Manual · Montos asegurados · Publi · Tarifas). Producto: **Seguro INS Viajero con Asistencia Autoexpedible, registro SUGESE `P19-57-A01-972 V5`**. Si un dato no está ahí, NO se escribe en la app.

## Estado actual

- **EN PROD:** [appasistenciainsviajero.netlify.app](https://appasistenciainsviajero.netlify.app) (público) + [/agente/](https://appasistenciainsviajero.netlify.app/agente/) (consola).
- **LA MARCA SDI EN EL CORREO + ÍCONO NUEVO (25 ago 2026, commit `26dc702`, rollback tag `pre-correo-marca-25ago`):** JC pidió *"standarizar la imagen del envío de pólizas con la misma estética de aviso de cobro y cotizaciones de autos"*. La referencia real es el **correo de cotización de `COTIZADOR-AUTOS`**, que es el único de esa casa que quedó COMPLETO con el sistema SDI el 25 ago (`js/email-marca.js`); los de póliza activa y renovación tienen el pie pero **todavía cargan 5 barras a la izquierda y no llevan filete** — si algún día hay que volver a comparar, comparar contra el de cotización. Lo que quedó acá:
  - **Encabezado navy PLANO `#0c2340`** con el logo del INS como `<img>` hosteado, + **filete de marca 60/25/10/5** (`#0369A1` · `#0D9488` · `#EA580C` · `#C9A227`) debajo. 🔴 **Se fue el `linear-gradient`**: Outlook no dibuja degradados y el encabezado se veía distinto según el programa. Aviso conocido y aceptado (le pasa igual al correo de autos): **el segmento AZUL del filete queda en ~2,66:1 contra el navy y la barra se lee como de tres franjas**. Se publica con el color exacto del manual; se arregla con una regla de 1 px si JC lo pide.
  - **Los datos del viaje** dejaron los chips de colores apilados y pasaron a **bloques sobrios** (rótulo 10 px en versalitas con `letter-spacing:.1em` sobre una regla de 1 px). Cada viajero: nombre + **sello del número de póliza** al lado, y debajo tres celdas — Destino · Vigencia · **Gastos médicos**, esta última a 16 px `#0c4a6e` porque es la cifra que importa (antes era una línea de 13 px al final de la fila).
  - **El sello del número de póliza** (`chapaPoliza`) es el equivalente exacto de la placa del cliente en el correo de autos: banda navy arriba con **"NÚMERO PÓLIZA INS"** (8,5 px, `letter-spacing:.16em` — JC pidió ese rótulo y "un poco más grande" sobre el 6,5 px inicial) y el número abajo a 15 px. 🔴 **El ancho de la caja lo manda el NÚMERO, no la banda**: con 17 caracteres a 8,5 px queda holgado, pero un rótulo más largo o más grande estira la caja y deja de leerse como un sello.
  - 🔴 **Las celdas que el PDF no trajo NO se dibujan.** `tarjetaViajero` arma el renglón solo con lo que existe y reparte el ancho según cuántas queden (1 → 100 % · 2 → 55/45 · 3 → 36/34/30). Nada de guiones ni de "—".
  - **Pie navy con el logotipo SDI full color** como IMAGEN + nota legal de 3 líneas en `#94a3b8` (6,16:1; la licencia SUGESE es dato regulatorio). Es el **MISMO `sdi-logo-email.png` del cotizador**: si alguna vez se regenera, se regenera en los dos repos.
  - **Fuera los emojis y las barras de color a la izquierda** (`border-left:4px`). El único aviso del correo conserva la señal pero como `border-top:3px`. El `&#9888;` (⚠) se queda: es símbolo tipográfico, no emoji.
  - **Ícono de la PWA:** se quitó el punto verde y entró el INS al pie del avión, **en su propia banda** (`rgba(255,255,255,.085)`). Se presentaron 3 opciones —A escudo con línea fina · B solo la palabra · **C banda al pie**— y JC eligió la C. Se conservan el navy `#13233E` y el aro teal `#21625C` del ícono de hoy. El **maskable** lleva el dibujo al **80 % centrado** para caber en el círculo seguro (r=204,8 sobre 512) y va a sangre, sin esquinas ni aro. Convención de archivos que ya traía el proyecto y **no se cambió**: los tres "any"/apple son RGB opacos con las esquinas rellenas de BLANCO; el maskable es navy a sangre. `sw.js` subió a `viajero-v2` para que el `activate` barra el caché con los íconos viejos.
  - 🔴 **A quien ya tenga la PWA instalada le queda el ícono viejo.** El sistema lo hornea al instalar; cambiar los archivos no actualiza un acceso directo ya creado. Solo cambia para instalaciones nuevas — decirlo antes de que lo pregunte.
  - **`.gitignore` nuevo** con `mockups-rediseno/` y `mockups-envio/`: Netlify sirve este repo **tal cual, sin build**, así que un `git add -A` publicaría las maquetas. Misma familia del incidente de `selftest.html`.
  - ✅ **Smoke de JC OK el 25 ago 2026**: envío real y PWA reinstalada en su teléfono con el ícono nuevo. Cooldown hasta ~1 sep.
  - **Verificado en localhost 8960 antes del push:** consola sin errores de consola, `VEmail.buildHtml` con dos viajeros y con uno solo (filete, los dos logos, el sello, sin degradado, sin barra izquierda, sin emoji), **selftest 24/24 DESPUÉS del cambio**, los 8 assets 200, página pública sin desborde a 375 y 1280. La maqueta quedó en `mockups-envio/` (local): `index.html` compara el correo viejo y el nuevo lado a lado y `gen/despues.cjs` **corre el `agente/email.js` REAL**, así que la maqueta no puede mentir.
- **EL SALUDO CON TILDES (25 ago 2026, commit `b0e542e`):** salió de revisar el correo. `titleCase` (`parse.js:4`) partía las palabras con `\b`, y en JavaScript la frontera se define por `\w` = `[A-Za-z0-9_]`: **una letra acentuada NO es carácter de palabra, así que cuenta como frontera** y mete una mayúscula de más en medio del nombre. Verificado corriendo la función: `"MARÍA FERNANDA"` → `"MarÍA Fernanda"`, `"JOSÉ ANDRÉS"` → `"JosÉ AndrÉS"`, `"NÚÑEZ"` → `"NÚñEz"`. Solo toca a `sugerirNombrePila`, pero de ahí sale el **saludo del correo y de las DOS plantillas de WhatsApp** — lo veía el cliente. Con nombres sin tilde el bug es invisible, por eso sobrevivió meses. Ahora se parte por inicio de cadena, espacio o guion: `/(^|[\s\-])(\p{L})/gu`, el mismo regex ya probado en `COTIZADOR-AUTOS` (`poliza-extract.js`). **`selftest.html` subió a 26 checks** con una muestra de nombre con tildes (inventada, como todas las de ese archivo). 🔴 **El test PRUEBA el bug**: revirtiendo `titleCase` da `"MarÍA JosÉ"` y falla — comprobado con un script que evalúa las dos versiones (`mockups-envio/gen/prueba-tildes.cjs`, local). Verificado también que los compuestos con guion (`"ANA-MARÍA"`) salen bien y que los nombres sin tilde no cambian.
  - 🔴 **Trampa de la verificación, para no volver a caer:** el selftest en localhost seguía dando **24/24 con el arreglo ya puesto**. No era el arreglo: era el **service worker de la página pública**, que tiene scope `/` y también intercepta `/agente/`. Un `?bust=` en la URL refresca el HTML pero **NO** el `<script src="parse.js">`, que sigue saliendo del caché HTTP. Se destapó desregistrando el SW y, para la prueba definitiva, trayendo `parse.js` con `fetch(..., {cache:'no-store'})` y evaluándolo en la página. **Antes de concluir que un arreglo no funciona en localhost, comprobar qué archivo está corriendo de verdad.** Misma familia del gotcha del cotizador de autos con `typeof _parseCoberturas`.
- **REDISEÑO "TORRE DE CONTROL" + PWA (24 ago 2026, commit `712587b`, rollback tag `pre-rediseno-torre-24ago`):** la página pública se reescribió completa. Se presentaron **3 maquetas navegables** (Pasaporte editorial · Torre de Control nocturna · Brújula bento) y JC eligió **Torre de Control con la tipografía SDI** (Poppins títulos / Inter texto / JetBrains Mono cifras — pidió cambiar la Bricolage/IBM Plex original). Modo nocturno navy `#0A1626`, teléfonos como tablero de vuelos con etiquetas GRATUITO/DIRECTO/ALTERNO en mono, punto verde "Operativo 24/7". Ajustes de JC sobre la maqueta, todos aplicados antes del push: (a) botones del hero "Tengo una emergencia" y "WhatsApp asistencia" con el MISMO peso visual (ambos sólidos) y centrados; (b) las letritas tipo tablero partido del hero se quitaron ("no se entiende") → eyebrow legible; (c) 🔴 **el registro de cambios se quitó de la cara del cliente** ("eso no le interesa al cliente") — el historial completo vive SOLO en el footer de `/agente/`; la página pública ya NO lleva `<details>` de actualizaciones (excepción consciente a la regla del 5 ago). Contenido verificado línea por línea contra el **DERSA+CG V5 vigente** el mismo día: todos los datos de la app anterior estaban correctos; se corrigió UNA imprecisión (pérdida de equipaje: la restricción "viaje de regreso" aplica a la TEMPORAL y "vía terrestre" a la DEFINITIVA — antes se mezclaban) y se agregó contenido nuevo verificado: acceso directo "¿Qué le pasó?" (6 escenarios → deep-link al acordeón del trámite con `go(tab, accId)`), tabla de sublímites fijos (preexistencia US$10 000 riesgo de vida · pandemias US$7 500 · funerales US$3 000 · dental US$500 · acompañante US$100/día máx US$1 000 · retorno menores US$500 — universales para toda edad/opción), y 800-TeleINS para consultas desde CR. **Logo SDI compacto full color** (kit v1.2, copia `sdi-logo.svg` en la raíz) al pie sobre pastilla clara. Las 3 maquetas + visor móvil quedaron en `mockups-rediseno/` **sin commitear** (local only).
- **PWA instalable (mismo commit):** `manifest.webmanifest` (standalone, es, theme `#0A1626`) + `icons/` (192 · 512 · maskable-512 · apple-touch-icon-180, avión blanco sobre navy con punto verde, generados por SVG→PyMuPDF, script en scratchpad `icono.py`) + `sw.js` **red-primero con respaldo en caché** (con internet siempre la versión nueva — nada de páginas viejas pegadas; sin internet sirve la última copia buena, CDNs incluidos: la guía abre offline). Botón "Instalar app" real vía `beforeinstallprompt` (Android; en iPhone instrucciones Compartir→Agregar a inicio); la tarjeta de instalación se oculta si ya corre standalone. 🔑 **La identidad del agente persiste en el dispositivo** (`EFFECTIVE_SEARCH` + localStorage `viajero_ag_link`): al instalarse, la PWA abre SIN los parámetros del link, así que los parámetros de agente se guardan la primera vez que llegan y se restauran cuando la app abre sin ellos — el cliente de otro agente sigue viendo a SU agente instalado. `appUrl()` también lee `EFFECTIVE_SEARCH`. Fix de paso: la línea "Visítenos en…" se oculta SIEMPRE que el agente activo no tenga web (antes un link de autoservicio sin `?a=` filtraba la web de JC). E2E verificado en localhost (7 pestañas, deep-links, sheet, autoservicio, persistencia+restauración, share URLs, 4 íconos+manifest+sw 200, 0 overflow 375/1280, 0 errores) y smoke en PROD (sw `activated` en HTTPS, manifest 200, banner JC, logo SDI, 0 overflow). Pendiente: smoke de JC instalando en su iPhone/Android real.
- **Repo:** [jhernandez-vibecode/APP-ASISTENCIA-INS-VIAJERO](https://github.com/jhernandez-vibecode/APP-ASISTENCIA-INS-VIAJERO). Local: `C:/Users/segur/APP-ASISTENCIA-INS-VIAJERO`.
- **Consola `/agente/` (23 jun 2026, commits `2bc66bc` + `61a38b9`):** PDF→correo Gmail multi-viajero + 2 WhatsApp editables. Verificada (parser self-test 12/12).
- **Cross-sell público "Comprá/Recomendá":** live desde 3 jun 2026.
- **Personalización multi-agente COMPLETA (1 jul 2026, commits `732be18` + `c9693b9`):** el link de la app dentro del correo (botón "Abrir mi guía de emergencias") y de las plantillas WhatsApp (comodín `{Link}`) ahora usa `VAgent.publicLink()` → el cliente abre la pública con la identidad de SU agente. Además: campo `cotizaLink` en el perfil (CTA "Comprar de nuevo" con el código de intermediario de CADA agente, fallback al de JC) y cross-sell público personalizado (`salesUrl()`: `sales` precargado > web del agente `?aw=` > SDI). Verificado en preview (JC default, agente autoservicio y plantilla legacy). Para dar acceso a la consola a otro agente solo falta agregar su Gmail a `WHITELIST` en `config.js`. Icono "Regla de oro" = estrella dorada (antes alert-triangle). Banner ámbar de exclusividad SIN la coletilla "no es un recurso estándar del INS" (quitada 1 jul por decisión de JC: lo importante es que sepan que es del agente).
- **FIX permiso de envío Gmail (27 jul 2026, commit `45e58a0`):** un agente reportó `Gmail API 403 … ACCESS_TOKEN_SCOPE_INSUFFICIENT` al presionar Enviar. Causa: entró con Google sin marcar la casilla *"Enviar correo electrónico en tu nombre"* y `prompt:''` nunca se la volvió a mostrar (ver "Gotcha del consentimiento granular" abajo). Fix en `auth.js` + `app.js`: (1) `grantedSend()` verifica el scope realmente concedido en la respuesta del token; (2) si falta, `signIn()` reintenta solo con `prompt:'consent'`; (3) `VAuth.canSend()` + `grantSend()` nuevos en la API; (4) banner ámbar en la consola con botón "Conceder permiso de envío" (el login NO se bloquea: los canales de WhatsApp siguen sirviendo); (5) `enviar()` pide el permiso ANTES de preparar los adjuntos, montado sobre el clic del agente para que el navegador no bloquee la ventana emergente; (6) el 403 en el envío ahora reconsiente y reintenta (antes solo se atrapaba el 401); (7) el volcado JSON crudo de Google se traduce a un mensaje en español. Verificado en preview con los 3 caminos: sin permiso (0 llamadas a Gmail, banner reaparece), 403 con reconsentimiento (reintenta y envía) y camino feliz (1 envío, sin popups extra).
- **FIX 🔴 se perdían la tarjeta y el comprobante (3 ago 2026):** JC reportó que el correo llegaba solo con la póliza + condiciones generales + manual; la **tarjeta de asistencia nunca se adjuntaba**. Causa raíz: `onFiles()` recorría con `for…of` la **`FileList` viva** del navegador y hacía `await VParse.readPdfText(f)` DENTRO del ciclo; el handler del input ejecuta `fi.value = ''` apenas ese primer `await` cede el control, lo que **vacía la FileList en pleno recorrido** → el ciclo moría después del PRIMER archivo. Reproducido en el navegador: de 3 archivos elegidos quedaba **1** (`["poliza.pdf"]`). El mismo riesgo tenía el arrastre (`e.dataTransfer.files` se neutraliza al terminar el evento). Fix en `app.js`: `Array.from(fileList)` **sincrónico antes de cualquier `await`**. Además: (1) la clasificación detectada se recuerda en `f.vKind` (ahí sí hay texto del PDF); (2) chips por archivo con contador visible "N archivos que se van a adjuntar" + botón × para quitar uno (antes la pérdida era silenciosa); (3) el `accept` admite imágenes y se saltea el parseo de PDF si el archivo no es PDF; (4) `email.js` rotula cada adjunto con su **tipo MIME real** (antes todo iba como `application/pdf`, lo que rompería una tarjeta en JPG) y codifica el nombre con RFC 2047/2231 si trae acentos; (5) la lista "Documentos adjuntos" del correo se arma con lo que REALMENTE se cargó, para no prometer una tarjeta que no viaja. Verificado en preview: 3/3 archivos, 12/12 selftest, cabeceras MIME inspeccionadas.
- **Sesión 5–6 ago 2026 — el panel de WhatsApp y el cierre del envío (commits `e44ff99` → `8176d11`):** seis pedidos de JC en dos días, todos con previo de diseño aprobado antes de tocar código. Quedó: **panel de éxito** al enviar el correo, en el lugar de los botones (mata el doble envío); **íconos nuevos** en las 2 plantillas (el mundo pasa a avión, el avión a check); **`{Asegurados}`**, el bloque de pólizas con el nombre del asegurado debajo de cada una; **el teléfono del cliente como bloque destacado**, con el botón bloqueado mientras falte; **confirmación antes de agregar otro asegurado**; y **"🧹 Limpiar"** para preparar el envío del siguiente cliente. El detalle de cada pieza vive en "wa.js" y "app.js — UI"; la ventana modal quedó generalizada en `confirmar(o)`, reusada por el agregar-asegurado y el limpiar. Dos fallas silenciosas cerradas de paso: con el teléfono vacío el botón abría WhatsApp **sin destinatario**, y el número escrito **se borraba en cualquier `render()`** porque vivía solo en el input (ahora `state.tel`).
- **🔴 Los dos errores propios de esta sesión, para no repetirlos:** (1) **Atrapado en la verificación, no llegó a prod:** al cambiar el ejemplo del campo de teléfono de `506 8888 8888` a `8888 8888`, el link salía con `phone=` **sin código de país** y WhatsApp habría abierto un número inválido. Cambiar un `placeholder` cambió el contrato del dato. (2) **Sí llegó a prod y JC lo reportó al día siguiente:** `syncEnvio()` llenaba la póliza del mensaje **una sola vez** (`if (pol && !state.poliza)`), así que con un segundo asegurado el WhatsApp mostraba solo la del primero mientras el correo sí llevaba las dos adjuntas. **Lección transversal:** cuando un dato se DERIVA de una lista que puede crecer, el autocompletado tiene que SEGUIR a la lista en cada cambio, no llenarse en el primer pase — y congelarse solo si el usuario lo editó a mano. Mismo patrón vale para destinatarios, saludos y cualquier resumen en los cotizadores.
- **🔴 Tres reglas de esta sesión que NO se vuelven a discutir:** (1) **Los adjuntos NO se pueden mandar por WhatsApp desde la app.** `web.whatsapp.com/send` solo transporta texto y no tiene parámetro de archivos; lo único que manda documentos por programa es la WhatsApp Business Cloud API de Meta (cuenta de empresa verificada + servidor propio + plantillas aprobadas por Meta + cobro por conversación), incompatible con esta app sin backend. JC ya lo decidió: no volver a proponerlo como si fuera cuestión de código. (2) **Cada update se anota en el pie de la propia aplicación** — regla de JC del 5 ago que aplica a TODAS sus apps: `<details>` con "Última actualización: \<fecha\>" y el historial en lenguaje de usuario, lo más reciente arriba. Acá está en las DOS caras: al final de `agente/index.html` y dentro del `<footer>` de la pública, debajo del copyright (JC lo aprobó sabiendo que el asegurado lo ve). Fechas reales reconstruidas del historial: no inventar versiones. (3) **Una plantilla guardada en `localStorage` le gana a `WA_DEF`** — es lo PRIMERO que hay que preguntar si un agente reporta "no me sale X en el mensaje": tiene que darle "Restaurar".
- **CARGA POR ZIP DEL INS + un asegurado por póliza (24 ago 2026):** JC abrió con *"estoy tratando de mandar nueve viajeros a un mismo asegurado, no me despliega la opción de cargarlos de uno en uno, y cuando los cargo todos de una vez solo jala uno"*. Eran **dos fallas encimadas**, las dos reproducidas en el navegador antes de tocar nada:
  - **(a) La consola abría sin una sola zona donde soltar archivos.** `showConsole()` nunca creaba la primera tarjeta y el paso 1 dibujaba únicamente el botón fantasma, que además decía "+ Agregar **otro** asegurado" sin haber ninguno. Medido: **0 `.dropzone`** al entrar. No era regresión del rediseño del 21 ago — `showConsole()` nunca lo hizo; lo tapaba `nuevoEnvio()`, que sí llamaba a `pushViajero()`, así que después del primer "Limpiar" el problema desaparecía y no se veía nunca más en la sesión.
  - **(b) Cada zona de arrastre pertenecía a UN asegurado.** Los nueve PDF caían en la misma tarjeta y `Object.assign(v, VParse.extractAll(text))` se pisaba a sí mismo en cada vuelta del ciclo: **sobrevivían solo los datos de la última póliza leída** y el correo salía con un asegurado y dieciocho adjuntos huérfanos.
  - **Lo que quedó:** el paso 1 arranca con una **zona grande** que recibe el ZIP tal como lo manda el INS —uno, los nueve, la carpeta suelta o la carpeta comprimida— más los botones "Elegir archivos o ZIP" y "Elegir una carpeta". Módulo nuevo **`zip.js` (`window.VZip`)** con JSZip 3.10.1 pineado, que abre los ZIP **recursivamente** (un ZIP con los nueve adentro es el caso real del agente que comprimió la carpeta) y lee carpetas arrastradas vía `webkitGetAsEntry()`.
  - **🔑 La llave del reparto es el número de póliza del NOMBRE del archivo** (`VParse.polizaDeNombre`, `\d{4}VIA\d{9}`): la oferta-constancia y la tarjeta lo llevan de prefijo, así que se agrupa sin abrir un solo PDF y **solo se lee la póliza** (de la tarjeta no sale ningún dato y abrirla costaba segundos por asegurado). Nueve ZIP → nueve tarjetas en ~2 s. Regla firme: **lo que trae número de póliza SIEMPRE se va con su póliza**, aunque se haya soltado encima de otra tarjeta; la tarjeta de destino solo manda sobre lo que NO lo trae (el comprobante de pago). `tarjetaPara()` reusa la tarjeta que ya tiene esa póliza, así que **volver a soltar el mismo ZIP no duplica al asegurado**.
  - **Las Condiciones Generales que el INS repite dentro de CADA ZIP ya no se adjuntan nueve veces:** `classifyFile` reconoce dos tipos nuevos, `condiciones` (DERSA+CG) y `promo` (la lámina "HTML-Bienvenida"), y los aparta. Con nueve asegurados eso son **7,84 MB en vez de ~13,5 MB**. Lo apartado se lista con el motivo en un `<details>` — la pérdida callada de un archivo ya costó el incidente del 3 ago.
  - **Barra de progreso** (`pintarCarga()` sobre `#carga`) que se pinta **sin `render()`**: un redibujado en plena carga se lleva por delante la zona de arrastre y los archivos que el navegador todavía está leyendo. Más **guarda de reentrada** (`procesando`): soltar un segundo lote con el primero a medio abrir dejaba dos recorridos pisándose sobre el mismo `state`.
  - **Verificado con los 9 ZIP reales de JC**, seis escenarios: nueve juntos (9/9/18, ~2 s) · re-soltar los mismos (no duplica) · de uno en uno (1→9) · los nueve encima de una sola tarjeta (igual se reparten) · carpeta comprimida (9) · consola recién abierta (0 tarjetas pero CON zona). Correo: 9 filas, 9 montos distintos, 20 adjuntos. Selftest **24/24**. Celular 375 px sin desborde en los 3 pasos. Cero errores de consola.
- **🔴 El arrastre que no trae nada tiene que decirlo (24 ago 2026, mismo día):** JC volvió con *"también debe tener la opción de jalar los pdfs por aparte y estoy intentando y no me deja"*. Verificado: **la carga de PDF sueltos funcionaba** (arrastre, selector, con o sin número de póliza en el nombre, y mezclados con ZIP en el mismo arrastre). El problema eran otras dos cosas:
  - **(a) Fallo silencioso.** `procesarEntrada()` abría con `if (!brutos.length) return;` — un arrastre sin archivos no hacía **nada** y no decía **nada**. Y llega vacío en un caso común, no raro: **arrastrar archivos desde adentro de un ZIP abierto en el Explorador de Windows**, que no los extrae ni se los entrega al navegador. Desde el lado del agente eso se ve exactamente como "no me deja". Ahora `nadaLlegó()` pinta una caja ámbar **justo debajo de la zona de arrastre** (no en `#status`, que está al final de la pantalla y no se ve) explicando las tres salidas: arrastrar el ZIP entero, extraerlo primero, o usar el botón. Lo mismo cuando el lote no trajo ninguna póliza ni tarjeta. 🔴 **Regla:** toda puerta de entrada de archivos necesita una rama para "no llegó nada" — un `return` mudo es indistinguible de una app rota.
  - **(b) La pantalla gritaba ZIP.** El título decía "Arrastrá acá los ZIP tal como los manda el INS", los PDF sueltos aparecían en gris chiquito al final, y el botón fantasma decía "+ Escribir los datos a mano, **sin PDF**", que se lee como "acá no van PDFs". Regresión de descubribilidad introducida el mismo día: la pantalla vieja decía "Arrastrá la póliza, la tarjeta de asistencia y el comprobante". Ahora el título es "Arrastrá acá los documentos del INS" y las dos vías tienen el mismo peso; el botón es "Elegir PDF o ZIP" y `accept` lleva `.pdf` adelante.
  - De paso, `unica` en el reparto de sueltos **se recalcula en cada vuelta**: si el primer PDF sin número acaba de crear la tarjeta, el segundo cae en ESA y no abre un asegurado nuevo.
- **🔴 El parser era ciego a la mitad de las pólizas (24 ago 2026, hallazgo de paso):** al cargar nueve de golpe salieron **tres asegurados sin nombre**. Hay **DOS plantillas** de oferta-constancia y el parser solo conocía una:
  - *Sucursal Central* (serie **0201**): `Nombre o Razón Social: X Tipo de Identificación:` — la única que se leía.
  - *Sucursal Virtual* (serie **0221**): `DATOS DEL RIESGO Nombre: X N° Identificación:` — quedaba **sin cliente y sin cédula**.
  - Y en la plantilla Virtual **`Correo Principal:` viene VACÍO** y lo siguiente en el texto es la etiqueta `Cc:`, así que `extractCorreo` cargaba literalmente **`"Cc:"` como correo del cliente** — y de ahí pasaba solo al campo del destinatario. Ahora se valida que sea un correo de verdad. **Con un asegurado por envío casi no se notaba; nueve de golpe lo destaparon.**
- **Condiciones Generales sincronizadas con el INS (24 ago 2026):** `assets/condiciones.pdf` llevaba **seis semanas desactualizado**. Revisados los 70 ZIP de viajero de `Downloads` por fecha, el corte es nítido: hasta el **6 jul** el INS mandaba el DERSA+CG de 621 KB / 23 páginas; desde el **8 jul 2026** manda el de 344 KB / 24 páginas, y desde entonces todos. Comparados palabra por palabra antes de cambiar nada: **mismo registro SUGESE `P19-57-A01-972 V5`**, 90 % idéntico, y el único cambio de fondo es que *"Firma representante legal"* pasó a *"Instituto Nacional de Seguros · Cédula Jurídica 400000-1902-22"*. **Ninguna cobertura, exclusión ni monto cambió.** La procedencia y la fecha de sincronización quedaron anotadas en `config.js`, arriba de `STANDARD_DOCS`. En `C:/Users/segur/Viajero Asistencia/` se **agregó al lado** (no se sobrescribió el de mayo). 🔴 **Al ver un DERSA nuevo dentro de un ZIP, comparar y volver a sincronizar** — el INS lo cambia sin avisar.
- **🔴 `selftest.html` publicaba datos de un cliente real (24 ago 2026):** Netlify sirve el archivo tal cual, sin build, así que el nombre, la **cédula** y el **correo** de un asegurado quedaban visibles en `/agente/selftest.html`. Anonimizadas las dos muestras (sin tocar la ESTRUCTURA del texto, que es lo único que el test prueba) y dejado un aviso en el propio archivo. Misma familia que [[feedback_datos_clientes_en_comentarios_codigo_publico]]: **cualquier archivo del repo es público en estos proyectos**, también los de prueba.
- **REDISEÑO "un paso a la vez" (21 ago 2026, opción A):** JC abrió con *"la pantalla no me agrada, hay un botón doble de correo y no se entiende"*. El diagnóstico real eran DOS problemas encimados abajo: (a) `Vista previa` y `Enviar` lado a lado, del mismo tamaño — uno abría el correo de mentira y el otro lo mandaba de verdad; (b) justo arriba, los tres botones de canal (`Correo` / `WhatsApp emitida` / `WhatsApp directa`) **eligen** y no mandan nada, pero se veían igual que los que sí mandan: cinco botones casi seguidos y uno solo enviaba. Se presentaron **3 opciones en maquetas navegables** (A un paso a la vez · B escritorio de dos columnas · C tres bloques con barra fija) y JC eligió la **A**. Quedó:
  - `state.paso` (1 cargar · 2 revisar · 3 enviar) y `render()` dibuja UNA pantalla por vez (`pantalla1/2/3`). La barra de pasos dejó de ser un dibujo: `rail()` es la navegación y **los tres puntos se pueden tocar siempre** (`irA(n)`) — el paso 3 valida por su cuenta, así que nunca hay que dejar al agente encerrado en un paso. Los botones de avance (`botonSiguiente`) **nunca se bloquean**: los datos se pueden escribir a mano y hay clientes sin PDF.
  - **Paso 3:** los canales pasaron a ser tarjetas grandes con ícono + descripción (`CANALES` + `canalPick()`), con chip "lo normal" en Correo; abajo **un solo botón** cuya etiqueta lleva el destinatario adentro (`textoBoton()` es la ÚNICA fuente de esa etiqueta, compartida con `pintarBoton()` para que no se separen), y `resumenCorreo()` con atajos "cambiar" que devuelven al paso 2. **La vista previa bajó a un enlace** debajo del botón; en los canales de WhatsApp no aparece (ahí ya se había quitado el 6 ago porque duplicaba a Enviar).
  - **`⚙️ Mi información de agente` se fue al encabezado** (botón `#btn-agente` en `agente/index.html`, se revela en `showConsole()`): estaba arriba del trabajo del día y se llena una sola vez en la vida. `agentePanel()` ahora devuelve `''` si está cerrado, así la pantalla arranca directo en el trabajo.
  - **Atajo `sinPoliza()`** al pie del paso 1 para el cliente que compró por su cuenta: salta directo al paso 3 con canal `directa`. Sin él, el paso a paso dejaba sin salida al caso que no tiene PDF que cargar.
  - **Reparto entre pasos:** el paso 1 tiene la zona de arrastre y la lista de archivos; `viajeroCard()` (paso 2) **perdió la zona de arrastre** y quedó con los chips + los campos. El título de cada tarjeta es el nombre del asegurado, no "Viajero 1".
  - **Dos textos que se quedaban viejos, atrapados en la verificación:** el aviso bajo el botón del paso 2 decía "sin el correo del cliente…" con el correo ya escrito, y el encabezado de la tarjeta seguía diciendo "Asegurado 1" con el nombre ya tecleado — los dos se calculaban solo en `render()`. Se repintan desde `wire()` sin redibujar (`subPaso2()` sobre `#paso-sub`, y `#tit-v<id>`), igual que hace `pintarTel()`. 🔴 **Misma familia que la lección del 6 ago:** un texto DERIVADO de un campo tiene que SEGUIR al campo, no llenarse en el primer pase.
  - **Encabezado remedido con el dato de PRODUCCIÓN** (lección del 8 ago): con el engranaje nuevo, en celular el correo se comprimía a ~44 px ("jhe…"), que no dice nada; `#who` pasó a `hidden sm:inline` y en celular queda solo el ⚙️. Medido: **61 px** de header en 375 px y en escritorio, sin desborde horizontal.
  - **De paso:** `preview()` reventaba con un TypeError crudo si el navegador bloqueaba la ventana emergente (`window.open` devuelve `null`); ahora lo avisa en `#status`.
  - **NO se tocó:** el parser (`selftest.html` 12/12 verificado DESPUÉS del cambio), `email.js`, `wa.js`, la página pública, el correo ni los mensajes de WhatsApp. Verificado en localhost 8960 el flujo entero, incluido el **panel de éxito que reemplaza al botón** (sigue siendo lo que impide el doble envío), la franja verde al pasarse a WhatsApp, el modal de "agregar otro asegurado" y el de "limpiar".
- **MARCA SDI en la consola (8 ago 2026):** la consola era la única app de JC que entraba sin logo — solo un avión y el nombre — y sin pedir ninguna tipografía, así que se veía distinta en cada máquina. Aplicada la **tanda 5** del `PLAN-TIPOGRAFIA.md` del kit, con mockup comparativo aprobado por JC antes de tocar código. Quedó: **logotipo oficial** del kit v1.2 copiado INLINE y vectorizado (variante 08-header, wordmark blanco + barra en los 4 colores, en el encabezado; variante 02-compacto en color en la pantalla de login) + **filete de marca** de 4 px bajo el header con la proporción 60/25/10/5 + **tipografía de 3 roles** (Poppins en títulos vía regla CSS sobre `h1..h4`/`.t-display`, Inter en toda la UI y JetBrains Mono en las cifras, las dos por `tailwind.config`, que también registra los colores como `bg-sdi-azul`/`text-sdi-azul`). Los azules genéricos de Tailwind (`blue-700`/`blue-600` del punto del paso activo, el enlace del drop zone, el canal activo, el chip "tomado del PDF" y el botón de Google) pasaron al **azul SDI `#0369A1`**. Título del header: "Consola de envíos" + "Seguro Viajero con Asistencia"; el avión salió del encabezado (lo reemplaza el logo) y sigue solo en el favicon. **NO se tocó** la página pública, el correo, los WhatsApp, el favicon ni ningún color semántico (verde de envío/WhatsApp, ámbar de avisos). 🔴 **El fondo del header se queda en el navy de siempre:** el azul institucional ya se probó en el Cotizador (commit `1fb6978`) y JC lo descartó por verse demasiado claro — no volver a proponerlo. Aviso repetido del Cotizador: sobre el navy, el segmento AZUL de la barra del logo queda en ~2,66:1 y la barra se lee como de tres franjas; se publica con el color de marca exacto porque recolorear el logo es decisión de JC.
- **🔴 El `#who` del header crece al entrar (bug atrapado en la verificación, 8 ago):** el `<span id="who">` dice "uso interno" hasta que el agente se loguea y `app.js:59` lo reemplaza por **el correo**, que es mucho más largo. Con el bloque de título nuevo, en celular el flex comprimía el texto, el subtítulo se partía en **tres líneas** y el header pasaba de 61 a **126 px**. Fix: `whitespace-nowrap` en el bloque del título + `truncate max-w-[40%]` en el `#who`. **Lección transversal:** al medir un encabezado hay que medirlo con el dato de PRODUCCIÓN, no con el texto de arranque — el estado logueado es el que ve el agente el 99 % del tiempo.
- **FIX cross-sell por agente (2 jul 2026, commit `9d4020c`):** el cross-sell de la pública ("Cotizar mi próximo viaje" / "Compartí el seguro") le filtraba a otros agentes el link de compra de JC. Doble causa: (1) el `cotizaLink` del agente NO viajaba en el link público; (2) `getAgent()` copiaba el objeto base de JC y los campos ausentes en el link quedaban con el valor de JC. Fix: parámetro nuevo `ac` en `publicLink()` + `getAgent()` sin herencia + `appUrl()` preserva `ac` + footer web oculto si el agente no tiene web (ver detalle en "Página pública").

## Stack técnico

- **Frontend:** HTML + Tailwind CDN + Lucide CDN. Single-file en la pública; scripts clásicos (no ES modules) con globales `window.*` en la consola. **Sin build, sin npm.**
- **PDF:** `pdf.js` 4.4.168 (import dinámico desde cdnjs) en la consola.
- **Envío correo:** Google Identity Services (GIS) + Gmail API `gmail.send`. Mismo Client ID que los cotizadores.
- **Hosting:** Netlify, auto-deploy desde `main` (1-2 min). Sin backend, sin DB.

## Estructura del repo

```
APP-ASISTENCIA-INS-VIAJERO/
├── index.html              # App PÚBLICA del cliente (single-file, Torre de Control, ~1000 líneas)
├── manifest.webmanifest    # PWA: nombre, standalone, íconos
├── sw.js                   # Service worker red-primero + respaldo offline
├── sdi-logo.svg            # Logo SDI compacto full color (copia del kit v1.2) — pie de la pública
├── icons/                  # icon-192/512, maskable-512, apple-touch-icon (avión + INS al pie, 25ago2026)
├── img/                    # Las 2 imágenes del CORREO, con URL absoluta:
│   ├── ins-logo.png        #   logo INS del encabezado (mismo archivo que INS BLANCO.png)
│   └── sdi-logo-email.png  #   logotipo SDI full color del pie (mismo archivo del cotizador)
├── INS BLANCO.png          # Logo INS header de la página pública
├── favicon.ico             # Favicon de viaje (sirve para / y /agente/)
├── SKILL.md                # Este archivo
├── .gitignore              # 🔴 mockups-*/ — Netlify sirve el repo tal cual, sin build
├── mockups-rediseno/       # (LOCAL, ignorado) 3 maquetas del rediseño + visor móvil
├── mockups-envio/          # (LOCAL, ignorado) comparativa del correo viejo vs nuevo + 3 íconos
├── agente/                 # CONSOLA PRIVADA del agente
│   ├── index.html          # Markup + CDNs + includes
│   ├── config.js           # window.VCfg (Client ID, whitelist, links, docs, AGENT_DEFAULT + AGENTES)
│   ├── parse.js            # window.VParse (extracción PDF + clasificación)
│   ├── auth.js             # window.VAuth (login GIS + whitelist + token Gmail)
│   ├── agent.js            # window.VAgent (perfil de agente personalizable + link público)
│   ├── email.js            # window.VEmail (HTML navy + MIME + envío Gmail)
│   ├── wa.js               # window.VWa (plantillas WhatsApp editables + link)
│   ├── zip.js              # window.VZip (abre los ZIP del INS y las carpetas arrastradas)
│   ├── app.js              # window.VApp (UI: viajeros, canales, preview, enviar)
│   ├── selftest.html       # Asserts de VParse, 2 plantillas + tildes, muestras inventadas (26/26)
│   └── assets/
│       ├── condiciones.pdf # DERSA+CG V5 vigente, sincronizado 24ago2026 (auto-adjunto)
│       └── manual.pdf      # Indemnización paso a paso 6MB (auto-adjunto)
└── docs/superpowers/       # spec + plan de la consola
```

## Página pública (index.html) — pestañas

Inicio · Contactos de Emergencia · Reclamos y Reembolsos · Guía Wi-Fi Call · Coberturas y Exclusiones · Vigencia y Prórroga · **Comprá / Recomendá** (cross-sell ámbar). Branding INS + Licencia 08-1318 + pie SDI. WhatsApp Universal Assistance: `wa.me/5491167502557` (link al bot de asistencia, se mantiene; es la excepción a la regla "nunca wa.me" — no lleva texto con emojis).

**Pestaña Comprá / Recomendá (agregada 3 jun 2026):** CTA "Cotizar mi próximo viaje" → `https://www.segurosdelins.com/seguros-de-viaje` (o el link del agente, ver `salesUrl()` abajo) + "recomendar a un amigo", que comparte la página de ventas O la app por tres vías: WhatsApp `api.whatsapp.com/send?text=` (sin teléfono, solo texto), Web Share API y copiar link. La URL de "compartir esta app" se arma en caliente con `window.location.origin + window.location.pathname` → auto-detecta el dominio real, no hay dominio hardcodeado.

**Agente personalizable (link por agente):** la info del agente (nombre, licencia, web, copyright, banner de exclusividad, texto de recomendación, título de la pestaña) NO está hardcodeada — se marca con `data-ag="nombre|licencia|web-link|sales-link"` y `applyAgent()` la sustituye al cargar. Registro `AGENTES` (default `jc`, con `sales` = página de ventas) + `getAgent()` lee `?a=<id>` (precargado) o los parámetros de autoservicio (`?an` nombre `/&ar` rol `/&al` licencia `/&aw` web `/&ac` link de compra). **REGLA anti-fuga (2 jul 2026):** si el link trae CUALQUIER parámetro de autoservicio, `getAgent()` construye la identidad SOLO desde el link y NO hereda NADA del base JC — un campo ausente queda vacío, nunca con el dato de JC (antes se copiaba `Object.assign({}, base)` y los campos no sobreescritos filtraban la web/licencia/link de JC). Cross-sell: `salesUrl()` = `a.sales` (param `ac` = link de compra/cotización del agente con SU código de intermediario; o `sales` precargado de JC) > web del agente > `segurosdelins.com/seguros-de-viaje` literal (NO var — `applyAgent()` corre antes de esa sección). El footer "Visítenos en…" se oculta si el agente no trae web propia (no muestra la de JC). `appUrl()` preserva `a/an/ar/al/aw/ac`. Los WhatsApp de Universal Assistance NO son del agente y se dejan fijos.

**Guarda del `publicLink()` (agent.js):** al generar el link, un campo que quedó EXACTAMENTE igual al `AGENT_DEFAULT` (JC) NO viaja (`if (p.x && p.x !== D.x)`). Motivo: la consola prefija el perfil del agente nuevo con los datos de JC (incluido `cotizaLink` con el código 1101130); si el agente cambia su nombre pero olvida cambiar el link de cotización, sin esta guarda difundiría el código de JC bajo su propio nombre. Con la guarda, ese campo simplemente no viaja → el cross-sell cae a la web del agente o al genérico, nunca al código de JC. **Onboarding:** cada agente DEBE llenar su "Link de cotización INS" con su propio código; si lo deja en el default de JC, su cross-sell no venderá con su código.

**Gotcha Lucide (aplica a TODAS las single-file de JC que usan Lucide):** `lucide.createIcons()` convierte `<i data-lucide>` en `<svg>` cuyo `className` es objeto (SVGAnimatedString), NO texto → `icon.className.replace(...)` truena. Usar `classList.add/remove/toggle` para recolorar. Era un bug pre-existente en `switchTab`, corregido el 3 jun 2026.

## Marca en cada cara (REGLA 🔴)

Del kit (`C:\Users\segur\SDI-BRAND-KIT`), ratificado por JC: **son dos marcas y nunca compiten en el mismo bloque.**

| Cara | Marca | Qué lleva |
|---|---|---|
| `/agente/` (consola) | **Seguros Digitales SDI** | Logotipo SDI en header y login + filete de marca. **El INS no entra** — es la herramienta de trabajo del agente. |
| `/` (página del cliente) y el correo | **Seguros del INS** | Logo del INS arriba y visible, SDI al pie como plataforma. |

El **favicon es la excepción a la unificación**: cada app lleva el suyo (acá, el avión) para poder distinguirlas con varias pestañas abiertas. El favicon del kit es para avatares y redes de la empresa. Los logos se copian del kit y **van vectorizados** (las letras son `<path>`, no `<text>`): un SVG enlazado con `<img>` no puede cargar una fuente externa y el wordmark sale ~9 % más ancho.

## Consola `/agente/` — arquitectura

Módulos como globales `window.*` cargados en orden por `agente/index.html`. Contrato:

```
VParse.extractAll(text) -> { cliente, nombrePila, poliza, cedula, destino, gastosMedicos, vigenciaDesde, vigenciaHasta, correo }
VParse.classifyFile(filename, text) -> 'poliza' | 'tarjeta' | 'comprobante' | 'otro'
VParse.polizaDeNombre(filename) -> '0201VIA023182200' | ''   // la llave del reparto por asegurado
VParse.readPdfText(file) -> Promise<string>     // import dinámico de pdf.js 4.4.168
VZip.esZip(file) ; VZip.expandir(files, onPaso) -> Promise<File[]>   // abre los ZIP, recursivo
VZip.entradasDe(dataTransfer) -> Entry[]        // 🔴 llamar SINCRÓNICAMENTE dentro del handler
VZip.hayCarpetas(entradas) ; VZip.desdeEntradas(entradas) -> Promise<File[]>
VApp.procesarEntrada(files, destinoId?)         // única puerta de entrada de archivos
VAuth.init() ; VAuth.signIn() -> Promise<{email, token}> ; VAuth.ensureToken() ; VAuth.getToken()
VEmail.buildHtml(envio) ; VEmail.buildAndSend(envio, attachments, token) ; VEmail.fileToB64 ; VEmail.pathToB64
VWa.getTemplate(tipo) ; VWa.saveTemplate(tipo, txt) ; VWa.resetTemplate(tipo) ; VWa.buildLink(tel, txt, nombre, agente)
VAgent.get() -> perfil activo ; VAgent.save(p) ; VAgent.reset() ; VAgent.publicLink(p) -> string
VApp.state = { paso:1|2|3, viajeros:[Viajero], destinatarios:[string], saludo, tel, canal, sent, envio, cargando, descartes }
VApp.irA(1|2|3)     // navegacion entre pasos ; VApp.sinPoliza() // atajo al paso 3 con canal directa
Viajero = { cliente, nombrePila, poliza, cedula, destino, gastosMedicos, vigenciaDesde, vigenciaHasta, correo, files:File[] }
```

### config.js (window.VCfg)
- `GOOGLE_CLIENT_ID`: `255791314248-apgnrs0tiii72ogau5dpsjm2eie6d2hu.apps.googleusercontent.com` (mismo de los cotizadores, público por diseño).
- `GMAIL_SCOPE`: `gmail.send openid email profile`. `WHITELIST` (3 correos, comparación case-insensitive): `jhernandez@segurosdelins.com`, `tramites@segurosdelins.com`, `chernandez@seguros-ins.com` (dominio `seguros-ins.com` CORRECTO — confirmado por JC 1 jul: ese agente tiene su propio dominio, NO es typo).
- `APP_LINK`: la pública. `COTIZA_LINK`: `https://cotiza.ins-cr.com/frmDatosIncluir.aspx?P=99&A=1101130` (form INS, código intermediario 1101130).
- `STANDARD_DOCS`: condiciones.pdf + manual.pdf (auto-adjuntos). `EMERGENCIA`: contactos oficiales (`insinternacional@grupoins.com`, NO `ins-cr.com`).
- `AGENT_DEFAULT`: perfil del agente por defecto (JC) con campos discretos `{ id, nombre, rol, licencia, codigo, tel, whatsapp, correo, web, cotizaLink }`. Reemplaza al viejo `FIRMA`. `AGENTES`: registro de agentes precargados por `id` para el link público `?a=<id>` (por ahora solo `jc`). `cotizaLink` = form INS con el código de intermediario del agente (CTA "Comprar de nuevo" del correo; fallback `COTIZA_LINK` de JC).

### agente/agent.js (window.VAgent) — personalización del agente
- `get()` → perfil activo (localStorage `viajero_agente`, default = `VCfg.AGENT_DEFAULT`). `save(p)` (auto-genera `id`/slug si falta). `reset()` → vuelve a JC. `publicLink(p)` → arma el link que el agente envía a sus clientes.
- Lo usan: el correo (firma en `email.js`), la plantilla WhatsApp `directa` (token `{Agente}`) y el botón "Copiar link para clientes" de la consola.
- **Autoservicio:** un agente NO precargado no necesita editar código — su identidad viaja en el link como parámetros (`?an=`nombre `&ar=`rol `&al=`licencia `&aw=`web). Si está en `VCfg.AGENTES`, el link es corto (`?a=<id>`).

### zip.js (window.VZip) — el ZIP del INS y las carpetas
El INS manda **un ZIP por póliza** con tres cosas: la oferta-constancia (`*_054_540_*`), la tarjeta de asistencia (`*_Tarjeta_*`) y una copia del DERSA+CG. A veces suma una lámina `HTML-Bienvenida-*.jpg`. **JSZip 3.10.1 pineado** (cargado por `<script>` bajo demanda, no por `import()`: es UMD). `expandir()` abre los ZIP de forma **recursiva hasta 3 niveles** — un ZIP con los nueve ZIP adentro es exactamente lo que manda el agente que comprimió la carpeta entera — y descarta la basura de Windows/macOS (`__MACOSX`, `Thumbs.db`, ocultos). Las carpetas arrastradas se leen con `webkitGetAsEntry()` + `readEntries()` **en bucle hasta que conteste vacío** (devuelve como mucho 100 entradas por llamada; sin el bucle, una carpeta grande se lee a medias en silencio).

🔴 **`entradasDe(dataTransfer)` hay que llamarlo SINCRÓNICAMENTE dentro del handler del drop**, igual que `Array.from(fileList)`: `dataTransfer.items` se neutraliza al terminar el evento. Es la misma familia de la trampa del 3 ago 2026, pero con la otra colección viva.

### parse.js
Lee la capa de texto del PDF. **Dos plantillas de oferta-constancia**, y la cabecera es lo único que cambia: *Sucursal Central* (serie 0201) usa `Nombre o Razón Social:` … `Tipo de Identificación:`, y *Sucursal Virtual* (serie 0221) usa `DATOS DEL RIESGO Nombre:` … `N° Identificación:`; el nombre y la cédula se buscan con la primera y, si falla, con la segunda. 🔴 En la plantilla Virtual **`Correo Principal:` viene vacío** y le sigue la etiqueta `Cc:`, así que `extractCorreo` **valida que lo capturado sea un correo** o devuelve vacío. `classifyFile` devuelve `poliza` · `tarjeta` · `comprobante` · **`condiciones`** (DERSA+CG del ZIP, que NO se adjunta porque el correo ya lleva la suya) · **`promo`** (lámina publicitaria) · `otro`, y recorta las carpetas del nombre antes de mirar. Anclas: póliza `\d{4}VIA\d{9}` · cliente tras "Nombre o Razón Social:" hasta "Tipo de" · cédula tras "Número de Identificación:" · destino tras "Destino (s) del Viaje:" hasta "Motivo" · gastos médicos tras "Gastos Médicos y Adicionales:" → formateado `US$1.000.000` · vigencia "Desde:/Hasta:" · correo tras "Correo Principal:" (el primero que NO sea @segurosdelins.com). `nombrePila` = tokens después de los 2 apellidos (CR). Todo editable; si algo falla queda vacío (nunca inventar). Verificar con `agente/selftest.html` (**26/26**, cubre las dos plantillas y el nombre con tildes). 🔴 **Las muestras del selftest son INVENTADAS a propósito:** Netlify publica ese archivo tal cual y en él estuvieron seis semanas el nombre, la cédula y el correo de un cliente real. Nunca pegar ahí el texto de una póliza de verdad.

### email.js — plantilla del correo (marca SDI, tablas + estilos en línea)
Rehecha el **25 ago 2026**; ver "Estado actual" para el porqué de cada pieza. Orden: encabezado navy **plano** `#0c2340` con el **logo del INS** (`VCfg.LOGO_INS_URL`) → **filete 60/25/10/5** → saludo + confirmación verde (sin barra a la izquierda) → **los datos del viaje** (`tarjetaViajero`: nombre + `chapaPoliza` al lado, y debajo Destino · Vigencia · **Gastos médicos** a 16 px) → botón verde `#047857` "Abrir mi guía de emergencias" al link personalizado del agente activo (`VAgent.publicLink(A)`, fallback `APP_LINK`) → adjuntos → contactos de emergencia → aviso ámbar con `border-top:3px` → firma → **CTA "Comprar de nuevo un Seguro INS Viajero"** al `cotizaLink` del agente activo (fallback `COTIZA_LINK`) → **`pieSDI`** con el logotipo (`VCfg.LOGO_SDI_URL`). La **firma** se arma desde el agente activo (`VAgent.get()`), no hardcodeada. Helpers: `filete` · `rotulo` · `bloque` · `chapaPoliza` · `dato` · `tarjetaViajero` · `listaAdjuntos` · `pieSDI`. Tipografía **Space Grotesk + Inter** (la misma de los correos de autos; ojo que la app y la consola usan Poppins — JC lo sabe, quedó así a propósito para que los correos de la casa se vean iguales entre sí). Envío: Gmail API `users.messages.send`, MIME `multipart/mixed`, base64url, remitente = cuenta logueada — **esa mitad del archivo no se tocó**. Adjuntos: por viajero (póliza+tarjeta+comprobante) + condiciones+manual UNA vez.

🔴 **Las dos únicas imágenes del correo van hosteadas y con URL ABSOLUTA** (`img/ins-logo.png` y `img/sdi-logo-email.png`, servidas por el propio sitio). En correo no sirve la ruta relativa, ni el SVG, ni el base64: Gmail los bloquea. La regla vieja "correos SIEMPRE solo texto+color, nunca `<img>`" quedaba **corta** — lo que Gmail bloquea es SVG y base64, no un PNG hosteado, y así lo hace el cotizador desde el 25 ago. Si el sitio estrena subdominio propio, **hay que actualizar las dos URL de `config.js`** o los correos salen con dos huecos rotos.

### wa.js — 2 plantillas WhatsApp (editables)
`emitida` (póliza que emití yo) y `directa` (compra externa). Editables en la consola; "guardar como predeterminado" → `localStorage` clave `viajero_wa_<tipo>` (carga lo guardado, no el `WA_DEF`). Link SIEMPRE `https://web.whatsapp.com/send/?phone=...&text=...` — **NUNCA `wa.me`** (corrompe emojis). `{Nombre}` (cliente), `{Agente}` (nombre del agente activo), `{Poliza}` (número de póliza) y `{Link}` (link personalizado `VAgent.publicLink()`) se sustituyen al generar el link; las plantillas viejas guardadas en localStorage con la URL cruda también se personalizan (la URL base se reemplaza por el link del agente vía placeholder `String.fromCharCode(0)` — NUNCA escribir el carácter NUL crudo en el fuente: git trata el archivo como binario). El teléfono lo escribe JC (no viene en la póliza).

**Comodines `{Asegurados}` y `{Poliza}`:** `{Asegurados}` (6 ago, el que usan las plantillas por defecto) escribe el bloque de pólizas **con el nombre del asegurado debajo de cada una**, tal como viene del INS — ver el detalle en "Estado actual". Va SOLO en su línea, porque el bloque es multilínea. `{Poliza}` (5 ago) sobrevive para las plantillas ya guardadas: solo los números unidos en español por `unir()` ("A, B y C"). Los dos se caen enteros —línea incluida, con el salto sobrante colapsado— si no hay dato: nunca sale un "📄 Póliza N°" a medias (`VWa.conAsegurados()` / `VWa.conPoliza()`). 🔴 Ojo permanente: **una plantilla guardada en `localStorage` le gana a `WA_DEF`**, así que quien haya usado "Guardar como predeterminado" no ve los comodines nuevos hasta darle "Restaurar". Es lo primero que hay que preguntar si un agente reporta "no me sale X en el mensaje".

### app.js — UI
Login gate → **panel colapsable "⚙️ Mi información de agente"** (campos del perfil + Guardar/Restaurar + "Tu link para clientes" con Copiar/Ver) → **stepper "1 Cargar el PDF · 2 Revisión · 3 Enviar"** (se recalcula en cada `render()`; paso 1 ✓ al haber datos, paso 3 activo al haber destinatario, los 3 ✓ tras enviar) → lista de viajeros con "➕ Agregar viajero". Cada viajero: **drop zone que sirve para arrastrar O hacer clic** (input file oculto `multiple accept=pdf`) → clasifica y autocompleta. Datos del envío: destinatarios (default = correo del 1er viajero) + saludo (default = nombrePila del 1er viajero), editables. Canales: Correo / WhatsApp emitida / WhatsApp directa. Vista previa + Enviar.

**El teléfono del cliente como bloque (6 ago 2026):** es el ÚNICO dato que no sale del PDF y el único que falta al pasarse a WhatsApp, así que dejó de ser una línea igual a las demás: bloque ámbar con ícono mientras está vacío → verde confirmando `+50688221348` al escribirlo (`bloqueTel()` + `pintarTel()`, que repinta sin `render()` para no perder el foco). `setCanal()` le da el foco al entrar. El botón de acción queda **deshabilitado** diciendo "Escribí el teléfono del cliente" y pasa a verde "Abrir WhatsApp" cuando hay ≥8 dígitos; `enviar()` guarda igual por si acaso. **"Vista previa" se oculta en los canales de WhatsApp**: hacía exactamente lo mismo que "Enviar" (abrir el chat), eran dos botones para una sola acción. 🔴 `state.tel` vive en el estado: antes el número existía solo en el input y **cualquier `render()` lo borraba en silencio** (agregar un viajero, quitar un archivo).

**Confirmación al agregar otro asegurado (6 ago 2026):** `addViajero()` ya NO empuja: si la lista está vacía llama a `pushViajero()` sin preguntar (al primero siempre se lo agrega a propósito), y del segundo en adelante abre `pedirConfirmacionViajero()`. La ventana cuelga del `<body>`, **no de `#console`**, para que un `render()` no la borre; recuerda a quién ya se cargó, se cierra con Esc / botón "No" / clic afuera, y el foco arranca en "No" (Enter cancela). **Dos defensas contra el doble clic accidental, que era el motivo del pedido:** (a) la ventana va **centrada**, a ~380 px del botón, que vive arriba a la derecha — si apareciera debajo, el segundo clic caería sobre el "Sí"; (b) el clic de afuera NO cierra durante los primeros 400 ms, si no el segundo clic cerraría la ventana y el agente solo vería un parpadeo. Mientras está abierta, el overlay `fixed inset-0 z-50` intercepta el clic sobre "+ Agregar viajero" (verificado con `elementFromPoint`). `nuevoEnvio()` llama a `pushViajero()` directo: no pregunta, porque el agente acaba de pedir empezar de cero.

**Confirmación del envío (5 ago 2026):** al salir el correo, `state.envio` guarda `{to, viajeros, adjuntos, hora}` y el **panel verde de éxito OCUPA el lugar de los botones** Vista previa/Enviar (check SVG que se dibuja, destinatario, las 3 métricas y los botones "Seguir por WhatsApp" / "Enviar a otro cliente"). Que los botones desaparezcan no es cosmético: **es lo que impide reenviarle el correo dos veces al cliente.** Si el agente se pasa a un canal de WhatsApp, el panel se encoge a una franja verde (`bannerExito()`) y los botones vuelven, porque ese canal todavía necesita enviar. "Enviar a otro cliente" → `pedirLimpiar()` → `nuevoEnvio()` limpia todo y deja una tarjeta de viajero lista (ver "Limpiar para el siguiente cliente": ese botón está en el panel, en la franja y arriba como "🧹 Limpiar"). Las animaciones (`.v-check` / `.v-pop`) viven en el `<style>` de `agente/index.html`, con `prefers-reduced-motion` respetado.

**🔴 Gotcha FileList viva (aplica a CUALQUIER app de JC que cargue archivos):** `input.files` y `e.dataTransfer.files` NO son arrays ni copias — son colecciones vivas. Si se las recorre con `for…of` y hay un `await` dentro del ciclo, cualquier cosa que las vacíe mientras tanto (`input.value = ''`, el fin del evento `drop`) **corta el recorrido en silencio** y solo sobrevive el primer archivo. Regla: **`Array.from(fileList)` como primera línea, antes de esperar nada.** Este fue el bug del 3 ago 2026 (se perdían tarjeta y comprobante). Revisar el mismo patrón en los cotizadores que suben PDF.

**Hardening de sesión:** al enviar, `VAuth.ensureToken()` renueva el token si tiene >50 min; si Gmail responde 401 (sesión vencida), reconecta en silencio (`signIn()`) y reintenta el envío una vez. El encabezado de la consola usa degradado navy `linear-gradient(135deg,#0b2545,#13477e,#1c6fb8)` (igual al del correo).

## Multi-viajero
Un solo correo lista a TODOS los viajeros y adjunta los documentos de cada uno + condiciones/manual una sola vez. Cada viajero puede tener distinta opción → distinto monto de gastos médicos (se muestra por viajero).

## Flujo de trabajo (cómo hacer cambios)

1. Editar en `C:/Users/segur/APP-ASISTENCIA-INS-VIAJERO/`.
2. Verificar local: preview `asistencia-viajero` puerto 8960 (`.claude/launch.json`). Self-test del parser: `/agente/selftest.html`.
3. `git add` + `commit` + `push origin main` cuando JC autorice → Netlify auto-deploya.
4. **Sincronizar SKILL.md en las 3 ubicaciones** (ver abajo).

## Pre-requisito OAuth (producción)

**Gotcha del consentimiento granular de Google (27 jul 2026, causa del 403 "insufficient authentication scopes"):** Google presenta los permisos sensibles como **casillas que el usuario puede dejar SIN marcar**. Si un agente no marca *"Enviar correo electrónico en tu nombre"*, el login sale bien igual (entra a la consola, la whitelist lo acepta) pero el token NO trae `gmail.send` y Gmail responde **403 `ACCESS_TOKEN_SCOPE_INSUFFICIENT`** al enviar. Peor: `requestAccessToken({ prompt: '' })` reusa lo ya concedido y **nunca vuelve a preguntar**, así que el agente quedaba trabado para siempre. `enable_granular_consent:false` NO sirve — Google hizo obligatorias las casillas para todas las apps. La única salida es verificar el scope concedido y volver a pedirlo con `prompt:'consent'`. Ya implementado (ver abajo); si el mismo síntoma aparece en otro proyecto con Gmail API (cotizadores), el patrón es el mismo.

Para que el login funcione, el origen del sitio (`https://appasistenciainsviajero.netlify.app`) y, para pruebas, `http://localhost:8960`, deben estar en **"Orígenes de JavaScript autorizados"** del Client ID en Google Cloud Console. Sin eso, el login falla aunque el correo esté en la whitelist. JC confirmó (23 jun 2026) que el origen de Netlify ya está agregado. Si algún día se estrena subdominio propio, hay que agregar ESE origen también.

## Sincronización del SKILL.md (REGLA 🔴)

Este SKILL nació el 23 jun 2026 (commit `14b44fb`) y cubre las DOS caras: la pública del cliente y la consola `/agente/`. Al actualizarlo, sincronizar en las 3 ubicaciones:

1. **Repo (GitHub):** `C:/Users/segur/APP-ASISTENCIA-INS-VIAJERO/SKILL.md`
2. **Skill instalado (Claude Code):** `C:/Users/segur/.claude/skills/especialista-asistencia-viajero/SKILL.md`
3. **Backup en Downloads:** `C:/Users/segur/Downloads/SKILL_APP_ASISTENCIA_VIAJERO.md`

```bash
cp C:/Users/segur/APP-ASISTENCIA-INS-VIAJERO/SKILL.md \
   C:/Users/segur/.claude/skills/especialista-asistencia-viajero/SKILL.md && \
cp C:/Users/segur/APP-ASISTENCIA-INS-VIAJERO/SKILL.md \
   C:/Users/segur/Downloads/SKILL_APP_ASISTENCIA_VIAJERO.md
```

## Pendiente / Fase 2
- 🧊 **Cooldown hasta ~1 sep 2026.** La jornada del 25 ago tocó el correo que ve el cliente, los cuatro archivos
  de ícono y el parser, todo en un día y todo con smoke de JC OK. Mientras dure, entra lo de siempre: documentación,
  cosméticos que no toquen el correo, y scripts de solo lectura. Nada de volver a mover la plantilla del correo ni
  `parse.js` sin un motivo nuevo.
- **Smoke de JC sobre la carga por ZIP (24 ago 2026)** — verificado en localhost 8960 con sus 9 ZIP, pero el **arrastre de una carpeta desde el explorador** solo lo puede ejercitar él (la prueba automática inyecta los archivos por código). Cooldown sugerido ~31 ago.
- Registro/estadísticas de envíos (localStorage) en la consola, con seguimiento — como los cotizadores.
- Subdominio propio tipo `asistencia-viajero.appsegurosdigitales.com` (opcional). 🔴 Si se estrena, actualizar
  `LOGO_INS_URL` y `LOGO_SDI_URL` en `agente/config.js` o los correos salen con dos huecos rotos.

## Reglas de trabajo con Juan Carlos (JC)
- JC NO programa. Necesita cambios completos y/o ya pusheados.
- Pushear a main directo cuando JC autorice ("listo", "ok", "push", "actualiza github").
- Mockups previos = archivo separado / widget, sin tocar el código en producción.
- Verificación obligatoria antes de marcar completo (self-test del parser y/o preview).
- Correos HTML: tablas + estilos en línea. **Nada de SVG ni base64** (Gmail los bloquea) y **nada de degradados** (Outlook no los dibuja). Un **PNG hosteado con URL absoluta SÍ sirve**, y así van los dos logos desde el 25 ago 2026. Sin emojis: cada programa de correo los dibuja distinto.
